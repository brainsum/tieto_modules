<?php

namespace Drupal\static_pages\Plugin\metatag\Group;

use Drupal\metatag\Plugin\metatag\Group\GroupBase;

/**
 * Provides a plugin for the 'Open Graph - Products' meta tag group.
 *
 * @MetatagGroup(
 *   id = "tieto_intra",
 *   label = @Translation("Tieto intra"),
 *   description = @Translation("These meta tags are for Tieto intranet."),
 *   weight = 1,
 * )
 */
class TietoIntra extends GroupBase {
  // Nothing here yet. Just a placeholder class for a plugin.
}
